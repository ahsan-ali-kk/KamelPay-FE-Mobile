import React from 'react';
// import {WithLocalSvg} from 'react-native-svg';

const LocalSvg = ({ width, height, asset, fill }) => {
        return null
        // return <WithLocalSvg width={width || 24} height={height || 24}
        //                      asset={asset} fill={fill || 'transparent'} />

};

export default LocalSvg
